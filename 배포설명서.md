# 학습노트 (Spotify 버전) — 배포 설명서

이 문서는 프로젝트 파일과 분리되어 있습니다. 앱 화면에는 이 내용이 전혀 나타나지 않습니다.

## 준비물
- GitHub 계정 (또는 Vercel CLI로 직접 배포도 가능)
- Vercel 계정 (무료, https://vercel.com — GitHub으로 가입 가능)
- Spotify 계정, 그리고 **재생을 하려면 Spotify Premium**이 필요합니다.
  (검색만 하는 건 무료 계정도 가능하지만, Web Playback SDK로 실제 소리를 내려면
  Premium이 필수입니다.)

## 1단계 — Spotify 앱 등록
1. https://developer.spotify.com/dashboard 접속 → 로그인 → "Create app"
2. 앱 이름/설명은 아무거나 입력 (사용자만 보는 값이라 위장과 무관합니다)
3. **Redirect URI**에 아래를 등록 (아직 배포 전이라면 3단계 이후 실제 도메인으로 다시 수정):
   `https://내도메인/api/callback`
4. API로는 "Web API"와 "Web Playback SDK" 둘 다 체크
5. 저장 후 대시보드에서 **Client ID**, **Client Secret**을 확인 (Secret은 "View client secret" 클릭)

## 2단계 — 코드 올리기
1. 받은 프로젝트 폴더(`index.html`, `api/`, `package.json` 등) 전체를 GitHub 새 저장소에 올립니다.
   (GitHub 데스크톱 앱이나 웹의 "Add file → Upload files"로도 가능, git 명령어 몰라도 됩니다.)

## 3단계 — Vercel에 배포
1. https://vercel.com 에서 "Add New → Project"
2. 방금 만든 GitHub 저장소 선택 → Import
3. Framework Preset은 "Other"로 두고 그대로 Deploy (별도 빌드 설정 필요 없음)
4. 배포가 끝나면 `https://프로젝트이름.vercel.app` 같은 주소가 생성됩니다.

## 4단계 — 환경변수 등록
1. Vercel 프로젝트 → Settings → Environment Variables
2. 아래 세 개를 추가:
   - `SPOTIFY_CLIENT_ID` = 1단계에서 받은 Client ID
   - `SPOTIFY_CLIENT_SECRET` = 1단계에서 받은 Client Secret
   - `SPOTIFY_REDIRECT_URI` = `https://프로젝트이름.vercel.app/api/callback`
3. 저장 후 Deployments 탭에서 "Redeploy" (환경변수는 재배포해야 반영됩니다)

## 5단계 — Redirect URI 맞추기
1. 다시 Spotify 대시보드로 돌아가서, 실제 배포 주소로 Redirect URI를 정확히 맞춰줍니다:
   `https://프로젝트이름.vercel.app/api/callback`
2. 1단계에서 가짜/임시 주소로 넣었다면 여기서 실제 주소로 교체 저장합니다.

## 사용법 (겉모습 그대로)
- 앱 접속 → 우측 하단의 작은 점 클릭 → "동기화 시작" 클릭
- Spotify 로그인 화면이 뜨고, 로그인하면 다시 앱으로 돌아옵니다 (이때부터 연결된 상태)
- "오늘의 단어를 입력하세요"에 곡 제목을 넣고 Enter → 결과 클릭 → 재생
- 하단 진행바 = 재생 타임라인, "이전 문제/다음 문제" = 이전곡/다음곡

## 자주 발생하는 문제
- **"Premium 계정이 필요합니다" 메시지** → Spotify Free 계정입니다. Web Playback SDK는
  Premium 전용이라 우회할 방법이 없습니다.
- **로그인 후 바로 튕김 / redirect_uri 오류** → Spotify 대시보드의 Redirect URI와
  Vercel 환경변수 `SPOTIFY_REDIRECT_URI`가 한 글자도 다르지 않아야 합니다
  (http/https, 끝 슬래시 유무까지 정확히).
- **검색은 되는데 재생이 안 됨** → 페이지를 새로고침 없이 몇 초 기다려보세요.
  Web Playback SDK가 기기를 등록하는 데 1~2초 걸릴 수 있습니다.
- **새로고침하면 로그인이 풀림** → 이 버전은 브라우저 저장소를 쓰지 않아서
  새로고침 시 다시 동기화해야 합니다. 필요하면 서버 세션/쿠키 방식으로
  로그인을 유지하도록 확장할 수 있습니다 (요청하시면 추가해 드립니다).

## 완전히 숨기고 싶다면
- 프로젝트/저장소 이름을 "study-notes" 등 무난한 이름으로 지으세요.
  ("spotify"가 URL이나 저장소 이름에 들어가면 티가 납니다.)
- Vercel 무료 도메인 대신 원하는 도메인을 연결하면 URL도 더 자연스러워집니다.
